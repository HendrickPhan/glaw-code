# Hello World Server

Build a simple Go HTTP server that listens on port 8080 and responds with "Hello, World!" to any request.

Create `main.go` with a `main` function and package `main`. Import `fmt` and `net/http`.
