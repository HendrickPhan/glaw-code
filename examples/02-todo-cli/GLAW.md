# Todo CLI App

Build a CLI task manager that stores tasks in a todo.json` file in the current directory.

Create `main.go` with:
- `add "Buy groceries"` - adds a new task
- `list` - shows all tasks with status
- `done <id>` - marks task as completed
- `delete <id>` - removes a task

